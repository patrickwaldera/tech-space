import { Loading } from "../Loading"
import { LoadingContainer } from "./styles"

const LoadingScreen = () => {
  return (
    <LoadingContainer>
        <Loading />
    </LoadingContainer>
  )
}

export { LoadingScreen }